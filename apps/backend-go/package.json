{
  "name": "@mono-repo/backend-go",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "go run cmd/server/main.go",
    "build": "go build -o bin/server ./cmd/server",
    "lint": "go vet ./...",
    "test": "go test ./...",
    "clean": "rm -rf bin"
  }
}
